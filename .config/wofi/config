# ---- Wofi Everforest Squared ----

show=drun                # combine run + desktop entries
prompt=>                 # matches ROFI arrow
location=center
width=780
lines=8
columns=2
allow_markups=true       # let Pango markup work if you use icons
allow_images=true        # enable entry icons
hide_scroll=false
insensitive=true         # case-insensitive search
normal_window=false      # use layer-shell (Wayland proper)
matching=fuzzy           # nicer than "contains"


style           = /home/jeri/.config/wofi/style.css
